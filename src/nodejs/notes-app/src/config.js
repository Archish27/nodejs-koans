/**
 * Why config is required?
 * To initialize necessary configs related to server. 
 * Let's say if you want to initialize environment variables it can initialize here.
 * 
 * Expected Goal;
 * 1. Use body-parser HINT: Require body-parser package. Read why it is required.
 * 2. Use url encoding as false. Question : Why encoding is used? What does it mean? 
 * 3. Use cors HINT: Require cors package.
 * 4. Export Config
 * 
 * Once done, head to router.js
 */

const config = (app) => {
    // Write here...
}

module.exports = config;
